# NovelGenerator 模型选择策略

> **专注中文小说创作的多模型混合方案**
>
> 最后更新：2026-02-06

---

## 核心原则

### 🇨🇳 中文创作优先
- **所有中文内容生成必须使用 DeepSeek**
  - 对话生成
  - 场景描写
  - 叙事连贯
  - 文笔润色

### 💰 成本优化策略
- **DeepSeek**：中文内容生成（$0.42/M输出，Cache Hit $0.028/M输入）
- **Gemini**：结构化任务，免费额度优先
- **OpenAI**：可选，用于特定场景

---

## 11个LLM调用环节分析

| # | 环节名称 | 主要任务 | 输入/输出 | 是否中文核心 | 是否需要JSON |
|---|---------|---------|-----------|------------|-------------|
| 1 | 故事大纲生成 | 创意构思 | 2K / 6K | ✅ 是 | ❌ 否 |
| 2 | 角色提取 | 结构化提取 | 10K / 2K | ⚠️ 次要 | ❌ 否 |
| 3 | 世界观提取 | 结构化提取 | 10K / 1K | ⚠️ 次要 | ❌ 否 |
| 4 | 主题元素提取 | 结构化提取 | 10K / 1K | ⚠️ 次要 | ❌ 否 |
| 5 | 章节计划生成 | 结构化规划 | 15K / 15K | ⚠️ 次要 | ✅ **是** |
| 6a | 结构Agent | 叙事框架 | 40K / 6K | ✅ **是** | ❌ 否 |
| 6b | 角色Agent | 对话生成 | 45K / 5K | ✅ **是** | ❌ 否 |
| 6c | 场景Agent | 场景描写 | 50K / 5K | ✅ **是** | ❌ 否 |
| 7 | 内容整合 | 文本整合 | 60K / 10K | ✅ **是** | ❌ 否 |
| 8 | 章节分析 | 结构化分析 | 12K / 2K | ⚠️ 次要 | ✅ **是** |
| 9 | 自我批评 | 质量检查 | 10K / 1K | ⚠️ 次要 | ❌ 否 |
| 10 | 编辑打磨 | 文笔润色 | 15K / 8K × 4轮 | ✅ **是** | ❌ 否 |
| 11 | 书名生成 | 创意文案 | 20K / 0.5K | ✅ 是 | ❌ 否 |

**中文核心环节（必须DeepSeek）：** 1, 6a, 6b, 6c, 7, 10, 11
**结构化任务（适合Gemini）：** 5, 8
**辅助提取（可选其他模型）：** 2, 3, 4, 9

---

## 方案对比总览

| 方案 | 定位 | 中文质量 | 成本/10章 | 免费额度依赖 | 推荐场景 |
|------|------|---------|----------|------------|---------|
| **方案A** | 极致经济 | ⭐⭐⭐⭐⭐ | **$0.48** | Gemini免费 | 日常创作、测试 |
| **方案B** | 最佳平衡 | ⭐⭐⭐⭐⭐ | **$0.40** | OpenAI+Gemini | 🏆 **推荐** |
| **方案C** | 精品出版 | ⭐⭐⭐⭐⭐ | **$1.20** | Gemini免费 | 出版级作品 |

---

## 方案A：极致经济方案

### 💰 定位
- **纯 DeepSeek + Gemini 免费额度**
- 成本最低，中文质量最佳
- 适合大量创作、快速迭代

### 🎯 模型分配

```typescript
const MODEL_CONFIG_A = {
  // 中文内容生成（DeepSeek）
  outline: 'deepseek-chat',        // 大纲生成
  structureAgent: 'deepseek-chat', // 叙事框架
  characterAgent: 'deepseek-chat', // 对话生成
  sceneAgent: 'deepseek-chat',     // 场景描写
  synthesis: 'deepseek-chat',      // 内容整合
  polish: 'deepseek-chat',         // 文笔润色
  title: 'deepseek-chat',          // 书名生成
  
  // 结构化任务（Gemini免费）
  planning: 'gemini-2.5-flash',    // 章节计划（JSON Schema）
  analysis: 'gemini-2.5-flash',    // 章节分析（JSON Schema）
  
  // 辅助提取（DeepSeek）
  extraction: 'deepseek-chat',     // 角色/世界观/主题提取
  critique: 'deepseek-chat',       // 自我批评
};
```

### 💰 成本计算（10章中文小说）

| 环节分类 | 模型 | Token消耗 | 成本 |
|---------|------|----------|------|
| 大纲生成 | deepseek-chat | 8K | $0.003 |
| 提取任务（3个） | deepseek-chat | 36K | $0.015 |
| 章节规划 | gemini-2.5-flash | 30K | **$0（免费）** |
| 章节生成（3个Agent） | deepseek-chat | 12M | $0.36 |
| 内容整合 | deepseek-chat | 1.4M | $0.04 |
| 章节分析 | gemini-2.5-flash | 140K | **$0（免费）** |
| 自我批评 | deepseek-chat | 110K | $0.01 |
| 编辑打磨 | deepseek-chat | 1.8M | $0.05 |
| 书名生成 | deepseek-chat | 20K | $0.008 |
| **总计** | - | **~15.4M tokens** | **$0.48** |

**Cache优化后：** 约 **$0.35**（输入Cache Hit节省90%）

### ✅ 优势
- ✅ 中文质量最佳（DeepSeek专长）
- ✅ 成本极低（$0.35-0.48/本）
- ✅ 利用Gemini免费额度处理JSON
- ✅ 简单稳定，易于维护

### ⚠️ 限制
- ⚠️ 完全依赖DeepSeek服务稳定性
- ⚠️ Gemini免费额度有限（每天1500请求）

---

## 方案B：最佳平衡方案 🏆

### 💎 定位
- **DeepSeek（中文） + OpenAI免费额度（规划） + Gemini免费（JSON）**
- 性价比最优，质量最佳
- **强烈推荐用于生产环境**

### 🎯 模型分配

```typescript
const MODEL_CONFIG_B = {
  // 创意规划（OpenAI免费额度）
  outline: 'gpt-5-nano',           // 25M/天免费，创意强
  title: 'gpt-5-nano',             // 25M/天免费，营销文案
  
  // 结构化任务（Gemini免费）
  planning: 'gemini-2.5-flash',    // 免费，JSON Schema稳定
  analysis: 'gemini-2.5-flash',    // 免费，结构化分析
  extraction: 'gpt-4.1-nano',      // 25M/天免费，提取准确
  critique: 'gpt-4.1-nano',        // 25M/天免费，批评能力强
  
  // 中文内容生成（DeepSeek）⭐核心
  structureAgent: 'deepseek-chat', // 中文叙事框架
  characterAgent: 'deepseek-chat', // 中文对话自然
  sceneAgent: 'deepseek-chat',     // 中文描写细腻
  synthesis: 'deepseek-chat',      // 中文连贯最佳
  polish: 'deepseek-chat',         // 中文文笔润色
};
```

### 💰 成本计算（10章中文小说）

| 环节分类 | 模型 | Token消耗 | 成本 |
|---------|------|----------|------|
| 大纲生成 | gpt-5-nano | 8K | **$0（免费）** |
| 提取任务 | gpt-4.1-nano | 36K | **$0（免费）** |
| 章节规划 | gemini-2.5-flash | 30K | **$0（免费）** |
| 章节生成（核心） | deepseek-chat | 12M | $0.36 |
| 内容整合 | deepseek-chat | 1.4M | $0.04 |
| 章节分析 | gemini-2.5-flash | 140K | **$0（免费）** |
| 自我批评 | gpt-4.1-nano | 110K | **$0（免费）** |
| 编辑打磨 | deepseek-chat | 1.8M | $0.05 |
| 书名生成 | gpt-5-nano | 20K | **$0（免费）** |
| **总计** | - | **~15.4M tokens** | **$0.45** |

**Cache优化后：** 约 **$0.32**

### ✅ 优势
- ✅ **中文质量最佳**（DeepSeek专注核心内容）
- ✅ **成本更低**（$0.32-0.45/本）
- ✅ **多厂商冗余**（服务稳定性高）
- ✅ **充分利用免费额度**
  - OpenAI: 25M tokens/天（规划和分析）
  - Gemini: 免费（JSON结构化）
  - DeepSeek: 付费但便宜（中文核心内容）

### 🎯 适用场景
- 🏆 **日常生产创作**（推荐）
- 📚 批量生成中文小说
- 💰 预算有限但追求质量
- 🔄 需要高可用性（多厂商备份）

### 📊 免费额度消耗分析
**OpenAI消耗（每本书）：**
- 大纲+提取+批评+书名 ≈ 200K tokens
- 每日额度：25,000K tokens
- **理论上限：每天125本书**

**Gemini消耗（每本书）：**
- 章节规划+分析 ≈ 170K tokens
- 免费额度：慷慨（无明确限制）
- 每天1500请求 → 每本约30请求 → **每天50本书**

**实际瓶颈：** DeepSeek成本（$0.32/本）

---

## 方案C：精品出版方案

### 🌟 定位
- **GPT-5顶级创意 + DeepSeek中文内容 + Gemini免费JSON**
- 创意规划用顶级模型，读者阅读内容用中文最佳
- 适合出版级、商业化作品

### 🎯 核心理念
> **"读者能看到的内容用DeepSeek，创意规划用GPT-5"**

- **GPT-5**：故事构思、章节规划（读者看不到但影响质量）
- **DeepSeek**：章节正文、对话、描写（读者直接阅读）
- **Gemini**：结构化数据提取（辅助工具）

### 🎯 模型分配

```typescript
const MODEL_CONFIG_C = {
  // 顶级创意规划（GPT-5）⭐
  outline: 'gpt-5',                // 故事构思最重要，用顶级模型
  planning: 'gpt-5',               // 章节规划影响整体质量
  
  // 中文内容生成（DeepSeek）⭐读者阅读
  structureAgent: 'deepseek-chat', // 叙事框架
  characterAgent: 'deepseek-chat', // 对话生成
  sceneAgent: 'deepseek-chat',     // 场景描写
  synthesis: 'deepseek-chat',      // 内容整合
  polish: 'deepseek-chat',         // 文笔润色
  title: 'deepseek-chat',          // 书名（中文）
  
  // 结构化任务（Gemini免费）
  analysis: 'gemini-2.5-flash',    // 章节分析
  extraction: 'gemini-2.5-flash',  // 角色提取
  
  // 质量控制（GPT-4.1）
  critique: 'gpt-4.1',             // 深度批评
};
```

### 💰 成本计算（10章中文小说）

| 环节分类 | 模型 | Token消耗 | 成本 |
|---------|------|----------|------|
| **创意规划（GPT-5）** | | | |
| 故事大纲 | gpt-5 | 8K输出 | $0.08 |
| 章节计划 | gpt-5 | 15K输出 | $0.15 |
| **中文内容（DeepSeek）** | | | |
| 章节生成（3个Agent） | deepseek-chat | 12M | $0.36 |
| 内容整合 | deepseek-chat | 1.4M | $0.04 |
| 编辑打磨 | deepseek-chat | 1.8M | $0.05 |
| 书名生成 | deepseek-chat | 20K | $0.008 |
| **结构化任务（Gemini）** | | | |
| 提取+分析 | gemini-2.5-flash | 180K | **$0（免费）** |
| **质量控制（GPT-4.1）** | | | |
| 深度批评 | gpt-4.1 | 110K | $0.88 |
| **总计** | - | **~15.4M tokens** | **$1.56** |

**优化后（使用GPT-5免费额度）：**
- GPT-5每天250K tokens免费 → 大纲+规划可免费
- **实际成本：约 $0.45**（仅DeepSeek部分）

### ✅ 优势
- ✅ **创意质量最高**（GPT-5顶级构思）
- ✅ **中文质量最佳**（DeepSeek正文内容）
- ✅ **适合商业化**（出版级质量）
- ✅ **成本可控**（利用GPT-5免费额度）

### 🎯 适用场景
- 📖 出版级作品
- 💼 商业化项目
- 🏆 参赛作品
- 🎨 追求极致质量

### 💡 性价比优化
如果充分利用GPT-5免费额度（250K tokens/天）：
- 大纲+规划 ≈ 50K tokens/本
- **每天可免费处理5本书的规划部分**
- DeepSeek部分 ≈ $0.45/本
- **总成本：$0.45/本**（与方案B相当，但创意更强）

---

## 方案选择建议

### 📊 决策树

```
开始
  ↓
是否需要出版级质量？
  ├─ 是 → 方案C（GPT-5创意 + DeepSeek中文）
  │        成本：$0.45-1.56/本
  │        质量：⭐⭐⭐⭐⭐
  │
  └─ 否 → 是否需要多厂商冗余？
           ├─ 是 → 方案B（OpenAI+DeepSeek+Gemini）🏆
           │        成本：$0.32-0.45/本
           │        质量：⭐⭐⭐⭐⭐
           │        稳定性：最高
           │
           └─ 否 → 方案A（DeepSeek+Gemini）
                    成本：$0.35-0.48/本
                    质量：⭐⭐⭐⭐⭐
                    最简单
```

### 🎯 快速推荐

| 用户场景 | 推荐方案 | 理由 |
|---------|---------|------|
| 个人创作者，日常写作 | **方案B** | 性价比最优，免费额度充足 |
| 工作室，批量生产 | **方案B** | 多厂商稳定，成本可控 |
| 出版社，精品图书 | **方案C** | 创意+中文双重最佳 |
| 预算极限，测试环境 | **方案A** | 最低成本，质量不打折 |
| 追求极致中文质量 | **任意方案** | 都用DeepSeek处理中文 |

---

## 实施建议

### 🔧 技术实现

#### 1. 环境变量配置
```bash
# .env
# DeepSeek（必须）
DEEPSEEK_API_KEY=your_deepseek_key

# Gemini（推荐，免费）
GEMINI_API_KEY=your_gemini_key

# OpenAI（方案B/C需要）
OPENAI_API_KEY=your_openai_key
```

#### 2. 模型配置文件
```typescript
// config/models.ts
export const MODEL_STRATEGY = {
  // 选择方案：'A' | 'B' | 'C'
  strategy: 'B',
  
  // 方案A配置
  strategyA: {
    chinese_content: 'deepseek-chat',
    json_tasks: 'gemini-2.5-flash',
  },
  
  // 方案B配置（推荐）
  strategyB: {
    creative_planning: 'gpt-5-nano',
    chinese_content: 'deepseek-chat',
    json_tasks: 'gemini-2.5-flash',
    analysis: 'gpt-4.1-nano',
  },
  
  // 方案C配置
  strategyC: {
    top_creative: 'gpt-5',
    chinese_content: 'deepseek-chat',
    json_tasks: 'gemini-2.5-flash',
    quality_control: 'gpt-4.1',
  },
};
```

#### 3. 环节映射
```typescript
export function getModelForTask(task: string): string {
  const strategy = MODEL_STRATEGY.strategy;
  
  // 中文核心内容（所有方案都用DeepSeek）
  if (['structure', 'character', 'scene', 'synthesis', 'polish'].includes(task)) {
    return 'deepseek-chat';
  }
  
  // 根据方案分配其他任务
  switch (strategy) {
    case 'A':
      return task === 'planning' || task === 'analysis' 
        ? 'gemini-2.5-flash' 
        : 'deepseek-chat';
        
    case 'B':
      if (task === 'outline' || task === 'title') return 'gpt-5-nano';
      if (task === 'planning' || task === 'analysis') return 'gemini-2.5-flash';
      if (task === 'extraction' || task === 'critique') return 'gpt-4.1-nano';
      return 'deepseek-chat';
      
    case 'C':
      if (task === 'outline' || task === 'planning') return 'gpt-5';
      if (task === 'analysis' || task === 'extraction') return 'gemini-2.5-flash';
      if (task === 'critique') return 'gpt-4.1';
      return 'deepseek-chat';
      
    default:
      return 'deepseek-chat';
  }
}
```

### 📈 监控建议

#### 成本追踪
```typescript
interface CostTracking {
  book_id: string;
  strategy: 'A' | 'B' | 'C';
  costs: {
    deepseek: number;
    openai: number;
    gemini: number;
    total: number;
  };
  token_usage: {
    deepseek: number;
    openai: number;
    gemini: number;
  };
  quality_score?: number;
}
```

#### 质量评估
- 定期对比不同方案的输出质量
- 收集用户反馈
- A/B测试章节质量

---

## 常见问题

### Q1: 为什么方案B比方案A便宜？
**A:** 因为充分利用了OpenAI的25M tokens/天免费额度，把规划、分析等非中文核心的任务转移到免费模型上。

### Q2: 方案C的GPT-5创意真的更好吗？
**A:** 对于故事构思、章节规划等创意密集型任务，GPT-5确实表现更好。但最终呈现给读者的中文内容，DeepSeek的表现最佳。

### Q3: Gemini免费额度够用吗？
**A:** Gemini每天1500请求，每本书约20-30请求（章节规划+分析），理论上每天可处理50-75本书。对于个人创作者绰绰有余。

### Q4: DeepSeek服务不稳定怎么办？
**A:** 方案B提供多厂商冗余。如果DeepSeek不可用，可临时切换到gpt-5-nano（中文质量略逊，但可用）。

### Q5: 能否动态切换方案？
**A:** 可以！建议在配置文件中设置策略开关，根据预算、质量需求、服务可用性动态调整。

---

## 附录：Token估算表

### 每本书（10章）Token消耗详情

| 环节 | 输入 | 输出 | 总计 | 占比 |
|------|------|------|------|------|
| 大纲生成 | 2K | 6K | 8K | 0.05% |
| 提取任务×3 | 30K | 6K | 36K | 0.23% |
| 章节规划 | 15K | 15K | 30K | 0.19% |
| 章节生成×10章 | 10M | 2M | 12M | 77.9% |
| 内容整合×10章 | 1.2M | 0.2M | 1.4M | 9.1% |
| 章节分析×10章 | 120K | 20K | 140K | 0.91% |
| 自我批评×10章 | 100K | 10K | 110K | 0.71% |
| 编辑打磨×10章 | 1.5M | 0.3M | 1.8M | 11.7% |
| 书名生成 | 20K | 0.5K | 20K | 0.13% |
| **总计** | **~13M** | **~2.5M** | **~15.4M** | 100% |

**核心发现：**
- 📊 **章节生成占78%** → 必须用DeepSeek（中文最佳+便宜）
- 📊 **编辑打磨占12%** → 必须用DeepSeek（中文润色）
- 📊 **内容整合占9%** → 必须用DeepSeek（中文连贯）
- 📊 **规划分析占2%** → 可用Gemini免费额度（JSON任务）

---

## 总结

### 🏆 最终推荐

**🥇 方案B - 最佳平衡方案**
- **成本：** $0.32/本（Cache优化后）
- **质量：** ⭐⭐⭐⭐⭐
- **稳定性：** 最高（多厂商冗余）
- **免费额度：** 充分利用
- **适合：** 90%的使用场景

**核心配置：**
```typescript
{
  创意规划: 'gpt-5-nano',         // 免费
  中文内容: 'deepseek-chat',      // 最佳
  JSON任务: 'gemini-2.5-flash',   // 免费
  质量控制: 'gpt-4.1-nano',       // 免费
}
```

---

**文档版本：** v1.0
**作者：** NovelGenerator Team
**更新日期：** 2026-02-06
